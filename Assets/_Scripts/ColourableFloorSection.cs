using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColourableFloorSection : MonoBehaviour
{
    // things are public because it's the end of the school year and i am tired
    // i mean something something rapid application development 
    public bool filledIn;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
